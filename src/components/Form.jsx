import React from 'react';


// Stateless Functional Component
const Form = () => (
    <form action="">
        <input type="text" />
        <button>Search</button>
    </form>
);

export default Form;